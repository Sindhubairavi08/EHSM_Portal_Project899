<mvc:View controllerName="ehsm.controller.Risk"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Page title="Risk Management" showNavButton="true" navButtonPress=".onNavBack">
        <content>
            <Table id="riskTable" 
                items="{
                    path: '/Z899_RISK',
                    sorter: { path: 'RiskID', descending: false }
                }"
                growing="true"
                growingThreshold="20"
                width="auto"
                class="sapUiResponsiveMargin">
                <headerToolbar>
                    <Toolbar>
                        <Title text="Risks" level="H2"/>
                        <ToolbarSpacer/>
                        <SearchField width="30%" search=".onFilterRisks" placeholder="Search by ID or Category"/>
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Risk ID" /></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Plant" /></Column>
                    <Column><Text text="Description" /></Column>
                    <Column minScreenWidth="Desktop" demandPopin="true"><Text text="Category" /></Column>
                    <Column minScreenWidth="Desktop" demandPopin="true"><Text text="Severity" /></Column>
                    <Column minScreenWidth="Desktop" demandPopin="true"><Text text="Likelihood" /></Column>
                    <Column minScreenWidth="Desktop" demandPopin="true"><Text text="Mitigation" /></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Date" /></Column>
                    <Column minScreenWidth="Desktop" demandPopin="true"><Text text="Created By" /></Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{RiskID}" />
                            <Text text="{Plant}" />
                            <Text text="{RiskDescription}" />
                            <Text text="{RiskCategory}" />
                            <ObjectStatus text="{RiskSeverity}" state="{= ${RiskSeverity} === 'High' ? 'Error' : ${RiskSeverity} === 'Medium' ? 'Warning' : 'Success'}" />
                            <Text text="{Likelihood}" />
                            <Text text="{MitigationMeasures}" />
                            <Text text="{path: 'RiskIdentificationDate', type: 'sap.ui.model.type.Date', formatOptions: { pattern: 'dd/MM/yyyy' }}" />
                            <Text text="{CreatedBy}" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
    </Page>
</mvc:View>
